This is a prototype/spike of what might become my idea of a reusable template for building an AJAX MVC application using jQuery and Simpler.

This project has no tests and no product backlog (yet).  It is not yet to be used for anything.  I'll attach a license if that changes.